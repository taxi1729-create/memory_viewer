/**
 * config.js - メモリビュアー設定ファイル
 * ============================================================
 * ★ このファイルが微調整の中心です。
 *    動作・デザイン・APIキーの変更はここから行ってください。
 * ============================================================
 */

const CONFIG = {

  // ----------------------------------------------------------
  // ★ スワイプ設定
  // ----------------------------------------------------------
  /** スワイプ検知の最小距離 (px)。小さいほど敏感になる */
  SWIPE_THRESHOLD: 50,

  /** フィード遷移アニメーションの時間 (ms) */
  TRANSITION_DURATION: 400,

  /** フィード遷移のイージング */
  TRANSITION_EASING: "cubic-bezier(0.25, 0.46, 0.45, 0.94)",


  // ----------------------------------------------------------
  // ★ Ken Burns（写真アニメーション）設定
  // ----------------------------------------------------------
  /** ズーム倍率 (1.0 = 等倍、1.08 = 8%拡大) */
  KB_SCALE: 1.08,

  /** 横移動量 (%) */
  KB_TRANSLATE_X: 1,

  /** 縦移動量 (%) */
  KB_TRANSLATE_Y: 0.5,

  /** 1サイクルの時間 (ms) */
  KB_DURATION: 8000,

  /** イージング */
  KB_EASING: "ease-in-out",


  // ----------------------------------------------------------
  // ★ 動画再生設定
  // ----------------------------------------------------------
  /** タップ時のアイコン（▶/⏸）表示時間 (ms) */
  ICON_DISPLAY_DURATION: 800,


  // ----------------------------------------------------------
  // ★ ハートボタン設定
  // ----------------------------------------------------------
  /** ハートアニメーション時間 (ms) */
  HEART_ANIM_DURATION: 300,

  /** ハートのアクティブカラー（CSS色文字列） */
  HEART_ACTIVE_COLOR: "#ff6b8a",


  // ----------------------------------------------------------
  // ★ 撮影情報の表示設定
  // ----------------------------------------------------------
  /** 撮影場所を表示するか */
  SHOW_LOCATION: true,

  /** 撮影日を表示するか */
  SHOW_DATE: true,


  // ----------------------------------------------------------
  // ★ Google AI (Gemini) API設定
  //    現時点では未使用。将来的な「場所自動推定」機能向け。
  // ----------------------------------------------------------
  API_CONFIG: {
    /** Google AI Studio で取得したAPIキーをここに記入 */
    GEMINI_API_KEY: "YOUR_GOOGLE_AI_API_KEY_HERE",

    /** 使用モデル */
    GEMINI_MODEL: "gemini-1.5-flash",

    /** 写真から場所を自動推定する機能 (true=ON) */
    USE_AI_LOCATION: false,
  },


  // ----------------------------------------------------------
  // ★ LocalStorage キー名
  //    データの保存場所を変更する場合はここを編集
  // ----------------------------------------------------------
  STORAGE_KEYS: {
    ALBUMS:  "mv_albums",   // アルバム情報
    HEARTS:  "mv_hearts",   // ハート済みメディアID
    CONFIG:  "mv_config",   // ユーザー設定
  },

};

// 外部ファイルから参照できるようにグローバル公開
window.CONFIG = CONFIG;
